#include "number.hpp"

#include <iostream>

#include <boost/multiprecision/cpp_int.hpp>
/*
enum rational_repr_mode {decimal, fraction};

class Rational: public Number {
private:
    bool negative;
    boost::multiprecision::uint128_t numerator;
    boost::multiprecision::uint128_t denominator;
public:
    Rational(bool neg, boost::multiprecision::uint128_t num, boost::multiprecision::uint128_t denom) {
        negative = neg;
        numerator = num;
        denominator = denom;
    };

    std::string repr(rational_repr_mode mode) {
        switch (mode) {
            case decimal:
                return "hey";
            case fraction:
                return "hi";
        }
    }
};
*/
